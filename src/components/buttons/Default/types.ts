type ButtonDefaultCustomProps = {
    text?: string,
    icon?: React.ReactElement,
    styles?: {
        bg: `#${string}`,
        iconBg?: `#${string}`,
    },
    anchorHRef?: boolean,
}

export type {
    ButtonDefaultCustomProps
}